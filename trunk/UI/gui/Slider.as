package gui
{
    
    import mongoose.display.TextureData;
    
    public class Slider extends Component
    {
        public function Slider(texture:TextureData=null)
        {
            super(texture);
        }
    }
}